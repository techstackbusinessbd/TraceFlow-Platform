FROM node:20-alpine

WORKDIR /app

# Enable hot reload polling for Windows host volumes
ENV CHOKIDAR_USEPOLLING=true

COPY frontend/package*.json ./
RUN npm install

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host"]

